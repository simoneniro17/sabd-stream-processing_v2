installare maven
installare le estensioni di java e di maven in vscode
per compilare: 
cd flink
mvn clean package

Questo crea il jar, per il momento va spostatop manualmente nella cartella jobs.
Questa direcotry è montata come volume in flink client.
se ci sono due jar quale va spostato? flink-1.0-SNAPSHOT.jar
per lanciare il job da dentro il container:


flink run -c it.flink.StreamingJob jobs/flink-1.0-SNAPSHOT.jar -m jobmanager:8081

(i nomi sono ovviamente provvisori)\


